name: Build and Publish raylight-api

on:
  workflow_dispatch:
  push:
    branches:
    - main

env:
  AZURE_WEBAPP_NAME: raylight-api
  AZURE_WEBAPP_PACKAGE_PATH: "./publish"
  RAYLIGHT_API_APP_SETTINGS: ${{ secrets.RAYLIGHT_API_APP_SETTINGS }}

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            8.0.x
            9.0.x

      - name: Setup App Settings
        run: |
          echo "$RAYLIGHT_API_APP_SETTINGS" > ./src/WebApi/appsettings.json

      - name: Restore Solution
        run: dotnet restore ./RaylightApi.sln

      - name: Build Solution
        run: dotnet build ./RaylightApi.sln --configuration Release --no-restore

      - name: Publish Solution
        run: dotnet publish ./RaylightApi.sln --configuration Release --no-build --output '${{ env.AZURE_WEBAPP_PACKAGE_PATH }}'
        
      - name: Deploy Solution
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_PUBLISH_PROFILE }}
          package: ${{ env.AZURE_WEBAPP_PACKAGE_PATH }}
